import { createGlobalStyle } from "styled-components";

const GStyle = createGlobalStyle`
    * {
        margin: 0;
        padding: 0;  
    }
    li {
        list-style: none;
    }
`;

export default GStyle;
